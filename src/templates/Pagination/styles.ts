import styled from 'styled-components';

export const Pagination = styled.section`
    align-items: center;
    display: flex;
    justify-content: center;

    .disabled {
        pointer-events: none;
        cursor: default;
    }
`